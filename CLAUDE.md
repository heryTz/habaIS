# CLAUDE.md

- Never add `Co-Authored-By: Claude ...` or any AI attribution trailer to commit messages.
- Prefer `type` over `interface` in TypeScript.
- Use snake_case for file names (e.g. `my_component.tsx`, `my_utils.ts`).
